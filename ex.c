
#include <stdio.h>
#include <stdio.h>

int main(void)
{
    // int digit; char c1, c2, c3;
    // int n_after3 = 0;        // chars consumed after digit+3 chars
    // int n_to_eol = 0;        // chars consumed after skipping to end of line
    // int n_with_nl = -1;      // chars including exactly one newline, if present

    // int assigned = scanf("%d%1c%1c%1c%n%*[^\n]%n%*1[\n]%n", &digit, &c1, &c2, &c3, &n_after3, &n_to_eol, &n_with_nl);

    // printf("assigned=%d\n", assigned);
    // if (assigned == 4) {
    //     printf("digit=%d  c1='%c'  c2='%c'  c3='%c'\n", digit, c1, c2, c3);
    //     printf("after_3 chars=%d\n", n_after3);                      // here: 4
    //     printf("to_end_of_line=%d\n", n_to_eol);                    // here: 13 (for '4kojuxa hello')
    //     printf("including_newline=%d\n", (n_with_nl>=0)?n_with_nl:n_to_eol); // here: 14
    // } else {
    //     puts("parse error");
    // }
    // return 0;

    int p = 0;

    p = !p;
    printf("%d\n", p);
    p = !p;
    printf("%d\n", p);

}   